apiVersion: v1
kind: Service
metadata:
  name: multiple-repos-env10-product7-app1
  labels:
    app: multiple-repos-env10-product7-app1
spec:
  selector:
    app: multiple-repos-env10-product7-app1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
